package com.midi.pianomidi.ui.theme

import androidx.compose.ui.graphics.Color

// Neon Green Accent Colors (Primary)
val NeonGreen = Color(0xFF00FF88) // Bright neon green
val NeonGreenDark = Color(0xFF00CC6A) // Darker variant for hover/pressed states
val NeonGreenLight = Color(0xFF33FFAA) // Lighter variant for highlights

// Dark Theme Colors
val DarkBackground = Color(0xFF0A0A0A) // Very dark background
val DarkSurface = Color(0xFF1A1A1A) // Dark surface for cards
val DarkSurfaceVariant = Color(0xFF252525) // Slightly lighter for card borders

// Text Colors
val TextPrimary = Color(0xFFFFFFFF) // White text
val TextSecondary = Color(0xFFB0B0B0) // Light gray text
val TextTertiary = Color(0xFF808080) // Medium gray text

// Accent Colors
val OrangeAccent = Color(0xFFFF6B35) // Orange for Learn Piano card
val TealAccent = Color(0xFF00D4AA) // Teal for Free Play card

// Status Colors
val StatusActive = NeonGreen
val StatusInactive = Color(0xFF666666)

// Legacy colors (kept for compatibility)
val Purple80 = NeonGreen
val PurpleGrey80 = TextSecondary
val Pink80 = OrangeAccent

val Purple40 = NeonGreenDark
val PurpleGrey40 = TextTertiary
val Pink40 = OrangeAccent